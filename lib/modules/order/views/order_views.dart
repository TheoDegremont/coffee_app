import 'package:flutter/material.dart';

class OrderViews extends StatelessWidget {
  const OrderViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Order Views'),
      ),
    );
  }
}